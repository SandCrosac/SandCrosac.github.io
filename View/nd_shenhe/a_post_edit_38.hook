//<?
$shenhe_inc = get_plugin_inc('nd_shenhe');
//  检测是否开启帖子审核
if($shenhe_inc['shenhe']){
    if(NOW_GID != C("ADMIN_GROUP") && !is_forumg($this->_forum,NOW_UID,$fid)){
        // 切换到待审核状态
        $Thread->update(['shenhe'=>1],['tid'=>$post_data['tid']]);
        M("Chat")->sys_send(1,"帖子[ <a href='".HYBBS_URLA('thread',$post_data['tid'])."'>{$title}</a> ]需要审核");
    }
}