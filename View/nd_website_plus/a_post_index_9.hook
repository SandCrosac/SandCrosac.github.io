
//<? //手机版发帖图片追加
$pic = X('post.img');
if(!empty($pic)){
    $pic = array_filter(explode(',',$pic));
    foreach ($pic as $val){
        if(substr_count($content,$val)){
            continue;
        }
        $content .= '<p><img src="'.$val.'"></p>';
    }

}